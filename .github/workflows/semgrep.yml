name: Semgrep
on: ["pull_request", "push"]
jobs:
  semgrep:
    runs-on: ubuntu-latest
    name: Check
    steps:
    - uses: actions/checkout@v1
    - name: Semgrep
      id: semgrep
      uses: returntocorp/semgrep-action@experimental
      with:
        config: r/python.lang.correctness
